---
adr: 84
title: implicitStyles 하드코딩 분기 해체 — 5 spec 완전 SSOT 복귀
reviews:
  - round: 1
    ts: '2026-04-20T08:28:53.984Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: 'PROGRESSBAR_TAGS 분기 line 번호 부정확 — :1657 주장 vs 실제 :1566 분기 시작점'
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/implicitStyles.ts:1566
          (grep PROGRESSBAR_TAGS.has); ADR-084:157 / breakdown:12 주장 :1657
        root_cause: >-
          ADR-084 가 '분기 위치' 로 참조하는 line 1657 은 실제 effectiveParent 설정 내부 라인. 분기
          시작점은 line 1566. 독자가 line 1657 로 Read 하면 PROGRESSBAR_TAGS.has 조건문이 안 보임
        outcome: pending
      - id: m2
        severity: MEDIUM
        category: migration-cost-unquantified
        summary: R3 BC 수식화 부족 — SelectTrigger/Breadcrumbs inline-flex→flex 영향 범위 미수식
        evidence: >-
          ADR-084:115 R3 'inline-flex → flex Preview DOM 변화' 를 '의도된 시각 변화' 로 축소.
          SelectTrigger/Breadcrumbs inline context 사용처 grep 없음, 영향 파일 수/% 수식 없음
        root_cause: >-
          adr-writing.md 동적 seed #3 (BC 훼손 가능성 수식화) 에 따르면 'X% 사용자 영향 / 평균 Y 파일
          재직렬화' 수식 필요. R3 는 LOW 분류되었으나 inline-flex → flex 는 block-level 전환으로
          parent flow 영향 가능 — 실측 grep 필요
        outcome: pending
      - id: l3
        severity: LOW
        category: alternative-strawman
        summary: 대안 A/C HIGH 위험의 구체 파일/함수 인용 부재
        evidence: >-
          ADR-084:55 대안 A HIGH 'classification 체계 붕괴' 추상. ADR-084:73-76 대안 C
          HIGH 2 'spec 모델 확장 필요', '대규모 리팩토링' 추상
        root_cause: >-
          adr-writing.md seed #1 (HIGH+ 위험 명시 시 파일/함수 3곳 이상 인용) 부분 위반. 대안 A/C 는
          기각 대안이지만 HIGH 주장은 증거 기반이어야 함 (현 서술은 추상 개념 위주) — 기각 사유 부실 리스크
        outcome: pending
      - id: l4
        severity: LOW
        category: other
        summary: >-
          breakdown:164 '잘못' 표현 — ADR-083 원칙 (archetype 기준 리프팅) 무시 표현 (category:
          documentation-wording)
        evidence: >-
          docs/design/084-implicitstyles-branch-dissolution-breakdown.md:164
          'ADR-083 Phase 8 에서 display:"inline-flex" 로 리프팅한 것은 잘못'
        root_cause: >-
          ADR-083 의 당시 원칙은 archetype 기준 리프팅이었고 SelectTrigger
          archetype=button(inline-flex) 이므로 Phase 8 리프팅은 원칙 준수. ADR-084 는
          archetype vs 실제 구현 차이를 해소하는 것이지 이전 작업 '오류' 가 아님. '잘못' → 'archetype 기준
          vs 실제 구현 기준 불일치' 용어 권장
        outcome: pending
      - id: l5
        severity: LOW
        category: generator-extension-gap
        summary: >-
          ContainerStylesSchema 에 rowGap/columnGap 미지원 — ProgressBar 완전 이관 불가 명시
          누락
        evidence: >-
          packages/specs/src/types/spec.types.ts:59-93 rowGap/columnGap 필드 없음.
          breakdown:116-117 분기에 rowGap/columnGap 잔존하지만 Phase 0 Schema 확장 목록
          (flexWrap 만) 에 포함 안 됨
        root_cause: >-
          ADR-084 scope = 'layout primitive 리프팅' 이지만 rowGap/columnGap 은 Schema
          미지원으로 구조적 이관 불가. breakdown §A2 또는 §ContainerStylesSchema 확장 섹션에 '완전 이관
          불가 + 후속 ADR 대상' 명시 필요
        outcome: pending
  - round: 2
    ts: '2026-04-20T08:43:52.906Z'
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: ssot-violation
        summary: >-
          Breadcrumb child 주입 잔존 — '5 spec 완전 SSOT 복귀' 주장 과장. parent container 만
          해체, child 는 branch-owned
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/implicitStyles.ts:955-971
          (Breadcrumb child 에
          display/flexDirection/alignItems/width/minWidth/height/minHeight 직접
          주입). docs/adr/084:1 제목 / :92 Decision / :134 Consequence /
          docs/design/084-breakdown:169 §Phase A4 — 모두 parent effectiveParent 만
          다룸
        root_cause: >-
          ADR-084 가 scope 를 parent container 로 한정했지만 문구 레벨에서 '완전 SSOT 복귀' 로 과장.
          child 주입(width=measureTextWidth, height=spec.size, flexShrink/flexGrow
          등)은 비즈니스 로직으로 대안 C 경로가 필요하지만 본 ADR 에 미반영
        outcome: fixed
        addressed_in: >-
          a09c7dc9 — 제목 'parent container primitive 해체' 로 축소, R5 신설(child 주입 잔존
          MED), Negative 에 'parent SSOT + child branch-owned' 혼합 명시, breakdown
          §Phase A4 child 주입 분류 테이블 추가
      - id: m2
        severity: MEDIUM
        category: migration-cost-unquantified
        summary: >-
          Calendar/ProgressBar/Meter CSS 최종값 변화 risk 누락 — ADR R3 가
          SelectTrigger/Breadcrumbs 만 언급
        evidence: >-
          docs/adr/084:17 비대칭 테이블 / :109 Risks R3 / :142 Consequence /
          docs/design/084-breakdown:74 / :215 — Calendar/ProgressBar/Meter 는
          archetype=grid + spec=flex 로 CSS 도 grid→flex 변화. ADR-084 R3 는
          SelectTrigger/Breadcrumbs inline-flex→flex 만 언급. Preview/Publish 회귀 범위
          과소평가
        root_cause: >-
          3 spec 도 archetype grid 위에 spec display:flex 를 얹으므로 CSS cascade 최종값이
          변함. 추가 검증 결과: ProgressBar.css 는 grid-template-areas/columns/자식
          grid-area 적극 사용 → flex 전환 시 Preview 배치 구조적 깨짐 HIGH 수준
        outcome: fixed
        addressed_in: >-
          a09c7dc9 — R3 를 5 spec 전체로 확장, R3-A HIGH(ProgressBar/Meter
          grid-template cascade)+R3-B LOW(Calendar) 분리, '잔존 HIGH 위험 1건' 명시, Gate
          G5 신설(Phase A2 실측 강제, 깨짐 시 후속 ADR 이관), Negative 에 5 spec CSS 변화 전체 나열
  - round: 3
    ts: '2026-04-20T08:53:32.710Z'
    reviewer: codex
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: adr-structure-violation
        summary: 문서 내부 위험 판정 충돌 — Alternatives HIGH+ 0 vs Risks R3-A HIGH 1건
        evidence: >-
          docs/adr/084:65 대안 B HIGH+ 0 / :85 Risk Threshold 루프 불필요 / :124 R3-A
          HIGH / :128 '잔존 HIGH 위험 1건' — 같은 문서에서 동일 대안에 대한 HIGH 판정이 상반
        root_cause: >-
          Revision 1 에서 Phase A2 ProgressBar/Meter grid-template cascade 위험을
          R3-A HIGH 로 승격했지만 Risk Threshold 표는 기존 4 spec scope 평가 유지 → 내부 모순.
          Codex 제안: Phase A2 를 scope 분리 or Risk Threshold 를 5 spec 기준으로 재작성
        outcome: fixed
        addressed_in: >-
          8b481acd — Phase A2 를 본 ADR scope 에서 완전 분리 (Scope 섹션 + Risk Threshold
          '4 spec 기준' 주석 + R3-A 삭제 + G5 삭제 + '잔존 HIGH 0건' 정정)
      - id: m2
        severity: MEDIUM
        category: adr-structure-violation
        summary: breakdown 이 ADR 본문 축소 scope 와 미동기화 — 제목/롤백 계획/세션 분할이 5 spec 일괄 전제
        evidence: >-
          docs/design/084-breakdown:1 제목 '5 spec 완전 SSOT 복귀' / :80 Phase A2 일반
          실행 / :298 롤백 커밋 계획 P2 포함 / :312 세션 분할 B 세션 ProgressBar/Meter 실행
        root_cause: >-
          Revision 1 이 ADR 본문은 Phase A2 HIGH 로 조건부 이관 가능하게 열었으나 breakdown 은 옛
          제목/세션/커밋 계획 유지. 구현 세션이 breakdown 만 보면 5 spec 일괄 수행 흐름 회귀 가능
        outcome: fixed
        addressed_in: >-
          8b481acd — breakdown 제목 '4 spec, Phase A2 scope 외', 롤백 계획 P2 취소선 + '후속
          ADR' 표기, 세션 분할 3→2 세션 (4h), Phase A2 후속 ADR 실행 조건 섹션 신설
  - round: 4
    ts: '2026-04-20T08:58:47.871Z'
    reviewer: codex
    source: live
    issues:
      - id: l1
        severity: LOW
        category: adr-structure-violation
        summary: >-
          ADR 본문 scope 숫자 잔여 — line 44 '5 분기 (본 ADR scope)' + line 113 '5 spec 각
          독립 커밋' 이 Revision 2 축소 scope 와 미정합
        evidence: >-
          docs/adr/084:24-44 감사 결과 섹션 5 분기 표기 / :113 Decision 선택 근거 '5 spec 각 독립
          커밋'
        root_cause: >-
          Revision 2 에서 Phase A2 를 scope 분리했으나 감사 결과 테이블 + Decision 근거 항목의 숫자는 5
          로 잔존. 구현자가 scope 를 다시 넓게 해석할 여지
        outcome: fixed
        addressed_in: >-
          91ed37b5 — 감사 결과 섹션을 '4 분기 본 ADR + 1 분기 후속 ADR' 로 분할 재서술, Decision 선택
          근거를 '4 spec 각 독립 커밋' 으로 정정, 제목/Scope 와 전면 정합
      - id: l2
        severity: LOW
        category: adr-structure-violation
        summary: 잔존 분기 '40-1=39' 계산 근거 불명확 — 4 분기 해체 구조와 미정합
        evidence: >-
          docs/adr/084:166 Consequences Negative '잔존 분기 40-1=39: 본 ADR 은 4 분기만
          해체'
        root_cause: >-
          Revision 2 에서 본 ADR 이 4 분기 해체 + Phase A2 1 분기 후속 분리 구조로 전환됐으므로 40-4=36
          이 맞음. 40-1=39 는 왜 1 만 빠지는지 근거 부재 — 단순 표기 실수
        outcome: fixed
        addressed_in: 91ed37b5 — '잔존 분기 40-4=36 (Phase A2 1 + 비즈니스 로직 ~35)' 로 정정
      - id: l3
        severity: LOW
        category: adr-structure-violation
        summary: >-
          breakdown schema 확장 설명이 Phase A2/A4 병기 — Revision 2 에서 A2 가 scope 외인데
          병기 유지
        evidence: >-
          docs/design/084-breakdown:264-266 'ADR-084 scope 내 확장 (선행 작업 — Phase
          0) ... Phase A2/A4 에서 flexWrap: wrap/nowrap 필요'
        root_cause: >-
          Revision 2 Phase A2 scope 분리 이후에도 flexWrap 필요 이유 목록이 병기 상태 유지. 본 ADR
          에서 flexWrap 이 필요한 이유는 Phase A4 Breadcrumbs 의 nowrap 뿐
        outcome: fixed
        addressed_in: >-
          91ed37b5 — breakdown '(Phase A4 Breadcrumbs 의 nowrap)' 로 축소 + A2 wrap
          은 후속 ADR 로 분리 명시. ADR line 60 Soft Constraint 도 Breadcrumbs 기준으로 정합
---
# ADR-084 Review Log

## Round 1 — 2026-04-20 (reviewer: claude)

## ADR 리뷰 결과: ADR-084 implicitStyles 하드코딩 분기 해체

### 구조 검증
7/7 섹션 HIGH 품질 PASS — Context(Hard Constraints 5개), Alternatives(3 대안 × 4축 위험), Risk Threshold Check(테이블+루프 판정), Decision(근거 5개+기각 2개+design 포인터), Risks(R1-R4 ID 표+'잔존 HIGH 없음'), Gates(G0-G4).

### 동적 seed (adr-writing.md 2026-04-20)
- #1 HIGH+ 위험 파일/함수 3곳 인용: PARTIAL (대안 A/C HIGH 추상)
- #2 Spec/Generator 확장 질문: PASS (ADR:44 flexWrap 선행 확장 명시)
- #3 BC 훼손 X%/Y 파일 수식화: FAIL (R3 수식화 부재)
- #4 HIGH+ threshold → Phase 분리: PASS (대안 C 후속 ADR 분리)

### 코드 검증
7/8 VERIFIED, 1 UNVERIFIED: `:1657 PROGRESSBAR_TAGS 분기` → 실제 분기 시작점 `:1566` (line 1657 은 내부 effectiveParent 설정).

### 누락 위험
2건: rowGap/columnGap Schema 미지원으로 완전 이관 불가 명시 누락 (LOW), breakdown:164 '잘못' 용어 교정 (LOW).

### 종합
- 구조 PASS 7/7 HIGH
- 코드 7/8 VERIFIED
- 누락 위험 2건
- 권고: HIGH 없음, MED 2건 (line 1657→1566 정정, R3 BC 수식화), LOW 3건 (HIGH 추상 보강, '잘못' 용어, rowGap/columnGap 명시)
- 결론: **수정 후 승인 가능** — Proposed → Accepted 전에 M1/M2 수정 권장

## Round 2 — 2026-04-20 (reviewer: codex)

## Codex Round 2 리뷰 결과: ADR-084

### HIGH 1건 + MEDIUM 1건 지적 → 모두 fixed in commit a09c7dc9

#### HIGH: Breadcrumb child 주입 잔존
- 증거: `implicitStyles.ts:955-971` 에서 child Breadcrumb 에 display/flexDirection/alignItems/width/minWidth/height/minHeight 직접 주입
- ADR-084 는 parent effectiveParent 만 해체 → '5 spec 완전 SSOT 복귀' 주장 과장
- **반영**: 제목 'parent container primitive 해체' 로 축소 + R5 신설 + 분류 테이블

#### MEDIUM → HIGH 승격: Calendar/ProgressBar/Meter CSS 변화
- 증거: ADR R3 가 SelectTrigger/Breadcrumbs 만 언급. 3 spec 도 grid→flex 변화
- 추가 검증: `ProgressBar.css` grid-template-areas + 자식 grid-area 적극 사용 → flex 전환 시 Preview 배치 구조적 깨짐 HIGH
- **반영**: R3-A HIGH (ProgressBar/Meter) + R3-B LOW (Calendar) 분리, Gate G5 신설 (Phase A2 실측 강제 + 후속 ADR 이관 경로)

### Revision 1 결과
- ADR-084 scope 명확화: parent container primitive 만
- 잔존 HIGH 위험 1건 (R3-A, Gate G5 로 관리)
- Phase A2 실행 전 Chrome MCP 실측 강제
- 후속 ADR 후보: ContainerStylesSchema grid-template-areas/columns 확장, child layout hook (measureTextWidth / size-indexed)

## Round 3 — 2026-04-20 (reviewer: codex)

## Codex Round 3 리뷰 결과: ADR-084

### MEDIUM 2건 — 모두 fixed in commit 8b481acd

#### M1: 문서 내부 위험 판정 충돌
- Alternatives/Risk Threshold: 대안 B HIGH+ 0 + 루프 불필요
- Risks R3-A HIGH + '잔존 HIGH 1건'
- 해결: **Phase A2 를 본 ADR scope 에서 완전 분리**. Risk Threshold 에 '4 spec scope 기준' 주석, R3-A/G5 삭제, '잔존 HIGH 0건' 정정

#### M2: breakdown 과 ADR 축소 scope 미동기화
- 제목 '5 spec 완전 SSOT' (옛), 롤백 계획 P2 일반 실행 단계, 세션 B Phase A2 포함
- 해결: breakdown 제목 '4 spec, Phase A2 scope 외', 롤백 P2 취소선 + 후속 ADR 라벨, 세션 3→2 세션 4h, Phase A2 후속 ADR 실행 조건 섹션 신설

### Revision 2 결과
- ADR 내부 일관성 회복: Alternatives HIGH 0 ↔ Risks HIGH 0 ↔ Gates HIGH 0
- breakdown 완전 동기화 (제목/커밋 계획/세션 분할)
- Phase A2 완전 scope 분리 + 별도 후속 ADR 경로 명시 (Schema 확장 → spec 선언 → 실측 → 분기 해체)
- Codex Round 2 지적(Breadcrumb child + CSS 변화)은 Revision 1 에서 반영 유지

## Round 4 — 2026-04-20 (reviewer: codex)

## Codex Round 4 리뷰 결과: ADR-084

### LOW 3건 — 모두 fixed in commit 91ed37b5

#### L1: scope 숫자 정합성
- 감사 결과 섹션 '40 분기 중 5 분기' → '본 ADR 4 분기 + 후속 ADR 1 분기' 분할
- Decision 근거 '5 spec 각 독립 커밋' → '4 spec 각 독립 커밋'

#### L2: 잔존 분기 계산
- '40-1=39' → '40-4=36 (Phase A2 1 + 비즈니스 로직 ~35)' 정정

#### L3: breakdown flexWrap 설명
- Phase A2/A4 병기 → Phase A4 Breadcrumbs 단일 축소
- ADR line 60 Soft Constraint 도 Breadcrumbs 기준으로 정합

### Revision 3 결과
- 제목/Scope/Alternatives/Decision/Risks/Gates/Consequences/breakdown 전부 '4 spec / Phase A2 scope 외' 로 전면 정합
- type-check 영향 없음 (docs only)
- Codex Round 2/3/4 지적 전부 반영 완료
