---
adr: 91
title: 'utils.ts + cssResolver.ts Record<string, number> 해체 — Class A/B/C 3-분류 sweep'
reviews:
  - round: 1
    ts: '2026-04-20T15:46:01.238Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: R10 inputHeights 소비처 attribution 오류 — DateField/TimeField → DateInput
        evidence: 'apps/builder/src/builder/workspace/canvas/layout/engines/utils.ts:1965'
        root_cause: >-
          ADR table (line 28) 가 R10 소비처를 'DateField/TimeField' 로 기술했으나 실제
          utils.ts:1965 는 `if (tag === "dateinput")` 분기. DateField 는 R11
          dfHeights (utils.ts:2029) 에 이미 있음. Phase 3 이관 대상 spec 을 DateInput.spec
          (존재 확인) 으로 정정 필요.
        outcome: pending
      - id: l2
        severity: LOW
        category: other
        summary: primitives/index.ts re-export 추가 명시 누락
        evidence: >-
          packages/specs/src/primitives/index.ts
          (colors/radius/shadows/spacing/typography 6개 관행)
        root_cause: >-
          ADR Phase 1 (line 103) 이 `primitives/font.ts` / `primitives/icons.ts`
          신설만 기술. 기존 6개 primitives 관행은 index.ts 에서 re-export 하는 구조인데 해당 항목 누락.
        outcome: pending
      - id: l3
        severity: LOW
        category: risk-4axis-incomplete
        summary: Class B 순환 의존 없음 증거 Risks 섹션 미명시
        evidence: 'apps/builder/src/builder/workspace/canvas/layout/engines/utils.ts:36'
        root_cause: >-
          utils.ts 는 이미 `import { TabsSpec, TabPanelsSpec } from
          "@composition/specs"` 중이라 Class B 해체 시 순환 의존 발생 없음. 안전 증거를 Risks R1 에
          각주로 포함 권장.
        outcome: pending
---
# ADR-091 Review Log

## Round 1 — 2026-04-20 (reviewer: claude)

## ADR 리뷰 결과: ADR-091 utils.ts + cssResolver.ts Record 해체

### 구조 검증 (7/7 PASS HIGH)

Context / Alternatives (3) / 4축 위험 / Risk Threshold Check (HIGH+ 0) / Decision / 구현 상세 / Gates 전부 PASS HIGH.

### 코드 검증 (9/10 VERIFIED)

- primitives/ 디렉토리 존재 (colors/radius/shadows/spacing/typography) ✅
- specSizeField 헬퍼 존재 (implicitStyles.ts:104) ✅
- Class B 순환 의존 없음 (utils.ts:36 TabsSpec/TabPanelsSpec import) ✅
- ICON_SIZE_MAP 2곳 완전 동일 값 — 진짜 DRY 위반 ✅
- TABS_BAR_HEIGHT Object.fromEntries 파생 ✅
- R10 attribution UNVERIFIED — utils.ts:1965 실제 dateinput 분기 (ADR 은 DateField/TimeField 로 잘못 기술)

### 누락 위험 (3건)

MED 1 / LOW 2

### 종합 판정

- 구조: PASS (7/7 HIGH)
- 코드 정합: 9/10 VERIFIED
- 누락 위험: MED 1 / LOW 2
- 결론: **수정 후 재리뷰** (MED 정정 필수, LOW 권장)
