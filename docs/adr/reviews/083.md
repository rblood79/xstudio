---
adr: 83
title: Archetype Base Styles → Spec containerStyles 리프팅
reviews:
  - round: 1
    ts: '2026-04-20T05:07:35.089Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          "11 archetype" 표기 — 실제 ARCHETYPE_BASE_STYLES entry 12개 (tabs-indicator
          포함)
        evidence: >-
          docs/adr/083-archetype-base-styles-lifting.md:9 vs
          packages/specs/src/renderers/CSSGenerator.ts:50-116
        root_cause: >-
          tabs-indicator archetype이 0 spec 영향이라 본문 서술에서 제외. 테이블 entry 수와 영향
          archetype 수 구분 표기 필요
        outcome: pending
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          "ADR-078 Phase 5 (ListBox/Menu/Autocomplete) 완결" 주장 불일치 —
          Menu/Autocomplete는 containerStyles 인프라만 있고 display/flexDirection 리프팅
          미적용
        evidence: >-
          docs/adr/083-archetype-base-styles-lifting.md:33-34 vs
          packages/specs/src/components/Menu.spec.ts, Autocomplete.spec.ts (Grep
          display/flexDirection 결과 0)
        root_cause: 선례 인용 시 코드 확증 누락. 실제 리프팅된 spec은 ListBox + ListBoxItem 2개
        outcome: pending
      - id: m3
        severity: MEDIUM
        category: risk-4axis-incomplete
        summary: >-
          Factory 중복 주입 재발 위험 누락 — ADR-079 P3 계약(factory
          display/flexDirection/gap/padding 제거)과의 충돌 가능성 Phase별 감사 절차 부재
        evidence: >-
          docs/adr/083-archetype-base-styles-lifting.md:93-103 Risks 표 +
          docs/adr/README.md:17 ADR-079 Implemented 계약
        root_cause: spec.containerStyles에 archetype base 추가 시 factory와의 중복 주입 감지 Gate 필요
        outcome: pending
      - id: l4
        severity: LOW
        category: adr-structure-violation
        summary: >-
          R4 (ADR-082 Hard Constraint 해제) 에 대응 Gate 없음 — ADR-082 Addendum 작성 시점
          미명시
        evidence: >-
          docs/adr/083-archetype-base-styles-lifting.md:100 R4 vs :105-113 Gates
          G1~G5
        root_cause: R4 LOW로 분류돼 Gate 매핑 생략. Gate G1에 Addendum 작성 포함 권장
        outcome: pending
      - id: l5
        severity: LOW
        category: other
        summary: ADR-081 tokenConsumerDrift snap 재실행 절차 누락
        evidence: >-
          docs/design/083-archetype-base-styles-lifting-breakdown.md:30-38 Phase
          공통 절차
        root_cause: archetype 리프팅이 snap에 영향 가능 — Phase별 통과 조건에 편입 권장
        outcome: pending
      - id: l6
        severity: LOW
        category: other
        summary: 'pnpm build:specs → packages/shared regenerate 체인 명시 부재'
        evidence: >-
          docs/design/083-archetype-base-styles-lifting-breakdown.md:60-71 Phase
          1 검증
        root_cause: 빌드 체인 미명시로 stale CSS 검증 위험
        outcome: pending
      - id: l7
        severity: LOW
        category: other
        summary: Preview/Publish CSS cascade 영향 검증 누락
        evidence: >-
          docs/adr/083-archetype-base-styles-lifting.md:111 G3 Chrome MCP 4
          archetype
        root_cause: >-
          Preview iframe이 generated CSS 로드 — Builder/Preview/Publish 3경로 샘플링 명시
          권장
        outcome: pending
  - round: 2
    ts: '2026-04-20T05:19:34.166Z'
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: generator-extension-gap
        summary: >-
          ContainerStylesSchema/emitContainerStyles 가 archetype base 전부를 표현 못함 —
          box-sizing/cursor/user-select/transition/font-family/position/grid-template-*/nested
          selector 미지원. 원 ADR G5(Generator 테이블 삭제) 수행 시 모든 spec 에서 box-sizing 소실
          + breakdown Pilot fontFamily 는 schema 미지원 필드
        evidence: >-
          packages/specs/src/types/spec.types.ts:59-93 +
          packages/specs/src/renderers/CSSGenerator.ts:50-116 +
          CSSGenerator.ts:634 emitContainerStyles +
          docs/design/083-archetype-base-styles-lifting-breakdown.md:56
          (fontFamily)
        root_cause: >-
          원 ADR 이 'archetype base 전부 리프팅 + table 삭제' 를 목표로 설정했으나
          schema/Generator 확장이 선행되지 않음. 목표와 소유 인프라 사이 범위 불일치
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          'ADR-078 Phase 5 선례 (ListBox/Menu/Autocomplete)' 근거 불일치 — 실제 layout
          primitive 리프팅된 spec 은 ListBox + ListBoxItem 2개. Menu 는 색상/spacing 만,
          Autocomplete 는 빈 composition.containerStyles
        evidence: >-
          docs/adr/083-archetype-base-styles-lifting.md:19 / :82 vs
          packages/specs/src/components/ListBox.spec.ts:76 +
          ListBoxItem.spec.ts:49 vs Menu.spec.ts:72 + Autocomplete.spec.ts:123
        root_cause: >-
          선례 인용을 ADR-078 Phase 5 단일 흐름으로 묶음. 실제는 ADR-071(인프라) + ADR-078 Phase
          5(ListBox) + ADR-079 P1(ListBoxItem+schema.alignItems) + ADR-079
          P2(read-through) + ADR-080(layout engine) 분리 인용해야 정확
        outcome: fixed
      - id: m3
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          '65/65 spec 이 containerStyles 에 display 를 명시하지 않음' 문장이
          ListBox+ListBoxItem 기존 리프팅을 무시. 뒤의 'collection 5 중 일부 기존 적용' 논리와 충돌
        evidence: 'docs/adr/083-archetype-base-styles-lifting.md:11'
        root_cause: 영향 범위 재집계 누락. '잔여 63 spec' 으로 수정 필요
        outcome: fixed
  - round: 3
    ts: '2026-04-20T05:40:25.792Z'
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: generator-extension-gap
        summary: >-
          Rev 2 는 Skia consumer 가 아직 일반화되지 않은 상태 무시. `CONTAINER_STYLES_SPEC_MAP
          = { listbox: ListBoxSpec }` 단일 + `CONTAINER_STYLES_FALLBACK_KEYS = 4
          필드` 만 소비 → scope 8 필드 중 절반 + listbox 외 태그는 미구현 → 63 spec 리프팅해도 Skia 가
          읽지 않아 전체 무효
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/implicitStyles.ts:95-105
        root_cause: >-
          Rev 2 가 ADR-080 `resolveContainerStylesFallback` Implemented 상태를 '일반화
          완성' 으로 가정. 실제는 listbox 전용 + 4 필드 부분 구현
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          'InlineAlert 가 Skia block fallback 으로 가로 배치로 무너진다' 트리거 근거는 현 코드에 없음 —
          이미 `implicitStyles.ts:~2000` 에서 `display: parentStyle.display ??
          "flex"` 하드코딩 주입 중. 진짜 문제는 '하드코딩 파편화 + consumer 미일반화'
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/implicitStyles.ts:2000-2030
          (InlineAlert 분기)
        root_cause: Rev 2 Context 의 트리거 사례가 재현되지 않는 상태. 하드코딩 분기 존재 누락
        outcome: fixed
      - id: m3
        severity: MEDIUM
        category: adr-structure-violation
        summary: >-
          breakdown 이 Revision 2 본문과 불일치 — line 9 'Skia block fallback' / line
          11 'ADR-078 Phase 5 ListBox/Menu/Autocomplete' / line 161 세션 F
          'Generator 테이블 삭제' 가 Revision 1 잔재
        evidence: >-
          docs/design/083-archetype-base-styles-lifting-breakdown.md:9, :11,
          :161
        root_cause: 본문 Rev 2 재작성 시 breakdown 동기화 누락
        outcome: fixed
  - round: 4
    ts: '2026-04-20T05:46:22.748Z'
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: generator-extension-gap
        summary: >-
          Rev 3 Phase 0 설계가 Skia consumer 일반화 미완 —
          `resolveContainerStylesFallback` 호출 지점이 `implicitStyles.ts:716`
          listbox 분기 1곳뿐이라 lookup 범위/fallback key 확장해도 다른 태그는 spec
          containerStyles 를 읽지 않음. Phase 0 에 '어디서 모든 태그에 대해 fallback 을
          parentStyle 에 선주입할지' 명시 누락
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/implicitStyles.ts:716
          (유일 호출 지점) +
          docs/design/083-archetype-base-styles-lifting-breakdown.md:39 (Rev 3
          Phase 0 섹션)
        root_cause: >-
          Rev 3 Phase 0 이 `resolveContainerStylesFallback` 내부 lookup 만 확장. 호출 지점
          자체를 일반화하지 않음 → lookup 확장 효력 없음
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Phase 0 예시 코드 `TAG_SPEC_MAP[tag]` lookup 이 casing 불일치로 대부분 undefined —
          `implicitStyles.containerTag` 는 `toLowerCase()` 된 값 (inlinealert,
          listbox), `TAG_SPEC_MAP` 키는 PascalCase (InlineAlert, ListBox)
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/implicitStyles.ts:491
          (`.toLowerCase()`) +
          apps/builder/src/builder/workspace/canvas/sprites/tagSpecMap.ts:131+
          (PascalCase 204 키) +
          docs/design/083-archetype-base-styles-lifting-breakdown.md:58 (Rev 3
          Phase 0 예시)
        root_cause: Rev 3 예시 코드에서 casing 정규화 helper 명시 누락
        outcome: fixed
---
# ADR-083 Review Log

## Round 1 — 2026-04-20 (reviewer: claude)

## ADR 리뷰 결과: ADR-083 Archetype Base Styles → Spec containerStyles 리프팅

구조 7/7 PASS. 코드 검증 4 VERIFIED / 1 UNVERIFIED (archetype 수치) / 1 PARTIAL (선례 모수). 누락 위험 MED 1 + LOW 3.

**MEDIUM 3건 (반영 필수)**
- M1: ADR:9 archetype 수치 표기 (11 vs 12 entry)
- M2: ADR:33-34 ADR-078 Phase 5 선례 모수 과장 (Menu/Autocomplete는 layout primitive 리프팅 미적용)
- M3: Factory 중복 주입 재발 위험 누락 (ADR-079 P3 계약 위반 잠재, Gate 감사 필요)

**LOW 4건 (권장)**
- L1: R4 Gate 매핑 (ADR-082 Addendum)
- L2: ADR-081 tokenConsumerDrift snap 절차
- L3: build:specs → shared regenerate 체인
- L4: Preview/Publish CSS cascade 샘플링

**결론**: 수정 후 승인 가능 (HIGH 0).

## Round 2 — 2026-04-20 (reviewer: codex)

## Codex 외부 리뷰 (Round 2) — HIGH 1 + MED 2

**HIGH 해결 방향 채택 = 옵션 A (Scope 축소)**

- ADR 본문 재작성 (Revision 2) — 제목: 'Layout Primitive 리프팅' 로 명확화
- Scope 를 ContainerStylesSchema 현재 지원 8 필드 (display/flexDirection/alignItems/justifyContent/width/maxHeight/overflow/outline) 로 축소
- 비-layout 속성 (box-sizing/cursor/user-select/transition/font-family/position/grid-template-*/nested selector) 은 archetype table 유지 + 후속 ADR 로 분리
- 대안 B (archetype table 완전 삭제) 를 별도 대안으로 명시 + HIGH 2 (기술 + 마이그레이션) 로 기각
- breakdown Pilot 예시에서 fontFamily 제거 + '본 ADR scope 에서 리프팅되는 layout primitive 필드' 표 신설
- Generator 정리 Phase 가 '테이블 유지 + layout primitive double-emit 허용' 으로 변경

**MED 2 해결**

- 선례 인용 분리: ADR-071(인프라) / ADR-078 Phase 5 + ADR-079 P1(리프팅 2 spec) / ADR-079 P2 + ADR-082(Panel) / ADR-080(layout engine) 각각 명시
- 영향 범위 '65/65' → '잔여 63 spec' 재집계

**결론**: Revision 2 본문으로 재리뷰 진행 권장. HIGH 0 / MED 0 / LOW 잔존 가능 상태.

## Round 3 — 2026-04-20 (reviewer: codex)

## Codex 외부 리뷰 (Round 3) — HIGH 1 + MED 2

**HIGH 해결 = Revision 3 (Phase 0 신설)**

- ADR 본문 전면 재작성 (Revision 3)
- Phase 0 (Skia consumer 일반화) 를 Hard Constraint + Gates G0 + 대안 A 설명 + breakdown 에 추가
- `CONTAINER_STYLES_SPEC_MAP` → TAG_SPEC_MAP 기반 일반화
- `CONTAINER_STYLES_FALLBACK_KEYS` 4 필드 → 8 필드 (scope 10 = 기존 gap/padding + 추가 8)
- 기존 `containerTag === "..."` 하드코딩 분기 전수 감사 절차 = R0 신설
- 하드코딩 분기는 `parentStyle.X ?? ...` 패턴이라 spec fallback 선주입되면 자연 override → R8 (하드코딩 해체) 후속 ADR 이관

**MED 1 해결**

- Context 트리거 재정의: 'archetype hidden default + 하드코딩 파편화 + consumer 미일반화 3자 중첩' 으로 명확화. InlineAlert 는 하드코딩으로 해결된 '예외' 이지만 새 archetype/변경 시 재발 구조

**MED 2 해결**

- breakdown 전면 재작성 (line 5-11 배경 / 세션 표 / Phase 0 신설 섹션 / 참조 경로 갱신)

**결론**: Revision 3 = 구조적으로 실행 가능한 최초 상태. HIGH 0 / MED 0 (추가 잔존). 승인 가능 범위.

## Round 4 — 2026-04-20 (reviewer: codex)

## Codex 외부 리뷰 (Round 4) — HIGH 1 + MED 1

**HIGH 해결 = Phase 0 3 영역 수정 구체화**

1. `CONTAINER_STYLES_SPEC_MAP` → `LOWERCASE_TAG_SPEC_MAP` (build-time `new Map(Object.entries(TAG_SPEC_MAP).map([k,v] => [k.toLowerCase(), v]))`) + fallback keys 10 필드 (기존 4 + 신규 6 = display/flexDirection/alignItems/justifyContent/width/maxHeight/overflow/outline/gap/padding)
2. `getImplicitStyles` 진입부 (`implicitStyles.ts:491` 부근 containerTag 계산 직후) **공통 선주입 layer** 추가 — `const parentStyle = { ...resolveContainerStylesFallback(containerTag, rawParentStyle), ...rawParentStyle }` 로 모든 태그에 fallback 선주입
3. 기존 `implicitStyles.ts:716` listbox 분기의 `...resolveContainerStylesFallback("listbox", parentStyle)` 중복 호출 제거

**MED 해결 = casing 정규화 명시**

- Phase 0 예시 코드에 `LOWERCASE_TAG_SPEC_MAP` 명시. lookup 은 `.get(tag)` 형태 (tag 는 `containerTag.toLowerCase()` 결과 전제)

**R0 보강**

- 하드코딩 분기 감사 결과 `parentStyle.X ?? "값"` 패턴은 자연 호환. 직접 할당 패턴 (`gridlistitem`/`listboxitem` 등) 은 `?? parentStyle.X` 도입 또는 분기 제거. 수정 불가 태그는 whitelist 임시 제외 + spec 리프팅 scope 에서도 제외 (후속 R8)
- 기존 리프팅된 ListBoxItem 이 현재는 직접 할당 분기 (`listboxitem` line 768) 때문에 Phase 0 효력 무시됨 → Phase 0 에서 해당 분기 정리 필요

**Gate G0 보강**

- (a) Map 구축 + 10 필드 / (b) 공통 선주입 + 중복 호출 제거 / (c) 하드코딩 분기 감사 + 처리 / (d) Chrome MCP ListBox 외 + ListBoxItem 재검증 / (e) type-check + vitest 회귀 0 / (f) snap update (10 필드 확장 불가피)

**결론**: Rev 4 = 구현 착수 가능 상태. HIGH 0 / MED 0 (추가 잔존). 승인 가능.
