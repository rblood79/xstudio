---
adr: 906
title: Collection spacing 런타임 계약
reviews:
  - round: 1
    ts: '2026-04-24T01:29:24.248Z'
    reviewer: claude
    source: live
    issues:
      - id: l1
        severity: LOW
        category: evidence-missing
        summary: 'R1 HIGH 대응 칸에 package boundary 위반 구체 표현 부재 (seed #1 PARTIAL)'
        evidence: 'docs/adr/906-collection-spacing-runtime-contract.md:112'
        root_cause: >-
          R1 대응이 'G1에서 helper 위치와 import direction 검증'만 기재. boundary 위반의 구체
          import 방향(`packages/specs → apps/builder` 금지)이 R1 본문에 없어 Gate G1 실패 시
          후속 조치가 추상적. References 9 파일 + breakdown Phase 0/1 경로로 간접 보강됨.
        outcome: pending
      - id: l2
        severity: LOW
        category: migration-cost-unquantified
        summary: 'R2 BC 영향 수식화 부재 (seed #3 PARTIAL)'
        evidence: 'docs/adr/906-collection-spacing-runtime-contract.md:113'
        root_cause: >-
          Hard Constraint 5에 BC 원칙과 R2에 'fixture test 추가' 대응은 있으나, GridList 기존
          인스턴스 중 root padding 부재 비율 등 X% 수치가 없음. Gate G3 'style 부재 baseline 유지'
          조건이 측정 가능한 수치로 고정되지 않음.
        outcome: pending
  - round: 2
    ts: '2026-04-24T01:32:51.697Z'
    reviewer: claude
    source: live
    issues:
      - id: l1
        severity: LOW
        category: evidence-missing
        summary: 'R1 HIGH 대응 칸 package boundary 위반 구체 표현 부재 (seed #1 PARTIAL)'
        evidence: 'docs/adr/906-collection-spacing-runtime-contract.md:117-123'
        root_cause: >-
          round 1 에서 R1 대응이 'G1 에서 helper 위치와 import direction 검증' 만 기재.
          boundary 위반의 구체 import 방향과 검증 명령이 본문에 없어 Gate G1 실패 시 후속 조치가 추상적이었음.
        outcome: fixed
      - id: l2
        severity: LOW
        category: migration-cost-unquantified
        summary: 'R2 BC 영향 수식화 부재 (seed #3 PARTIAL)'
        evidence: 'docs/adr/906-collection-spacing-runtime-contract.md:125-130'
        root_cause: >-
          round 1 에서 Hard Constraint 5 BC 원칙과 R2 'fixture test 추가' 대응은 있었으나,
          GridList 기존 인스턴스 중 root padding 보유 비율 등 X% 수치가 없었음. Gate G3 'style 부재
          baseline 유지' 조건이 측정 가능한 수치로 고정되지 않았음.
        outcome: fixed
  - round: 3
    ts: '2026-04-24T01:38:23.703Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Context 주장 3 + breakdown Phase 1 data-layout 작업이 RAC auto-emit 으로
          중복/불필요
        evidence: >-
          react-aria-components@1.15.1 dist/GridList.mjs:198 +
          docs/adr/906-collection-spacing-runtime-contract.md:15 +
          docs/adr/design/906-collection-spacing-runtime-contract-breakdown.md:58-59
          + docs/adr/906-collection-spacing-runtime-contract.md:137
        root_cause: >-
          RAC GridList 내부 root div 가 무조건 `"data-layout": layout` 속성을 auto-emit
          (node_modules/.../GridList.mjs:198). composition GridList.tsx 가 직접
          명시하지 않아도 DOM 속성은 달린다. 따라서 (a) ADR Context 주장 3 '[data-layout="grid"]
          selector 미반영' 서술은 독자를 오도하고, (b) breakdown Phase 1 '모든 AriaGridList 반환
          경로에 data-layout 수동 추가' 지시는 중복 작업이며, (c) G2 Gate 'data-layout 이 Preview
          DOM/CSS 에 반영된다' 조건은 RAC 기본동작 검증으로 퇴화한다. 실제 결함은 Preview
          renderer(renderGridList) 가 root element.props.style 을 전달하지 않아
          style.padding/gap 편집이 DOM 에 도달하지 않는 것뿐이다. Round 1 VERIFIED 판정은
          composition 파일만 grep 하고 상류 node_modules 를 추적하지 않은 검증 공백.
        outcome: pending
  - round: 4
    ts: '2026-04-24T01:40:06.640Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Context 주장 3 + breakdown Phase 1 + G2 Gate 의 data-layout 서술을 RAC
          auto-emit 실측에 맞춰 동기화
        evidence: >-
          docs/adr/906-collection-spacing-runtime-contract.md:15,137 +
          docs/adr/design/906-collection-spacing-runtime-contract-breakdown.md:58-59
        root_cause: >-
          round 3 에서 `react-aria-components@1.15.1` `dist/GridList.mjs:198` 가
          root div 에 `data-layout={layout}` 을 auto-emit 함을 확증. round 4 에서 3곳 동기화
          수정 완료: (M1) ADR Context 주장 3 을 'selector 가 이미 매칭 — 실결함은 Preview
          renderer 의 style 미전달' 로 재서술, (M2) breakdown Phase 1 의 '모든 AriaGridList
          반환 경로에 data-layout 추가' 지시를 '수동 추가 불필요 + RAC auto-emit' 으로 교체, (M3) G2
          Gate 통과 조건을 'style.padding/gap Preview inline style 반영 + RAC auto-emit
          data-layout 이 selector 와 매칭됨을 test 로 확인 (수동 추가 아님)' 으로 재정의.
        outcome: fixed
  - round: 5
    ts: '2026-04-24T02:01:48.018Z'
    reviewer: codex+claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: migration-cost-unquantified
        summary: R2 '0%' 단정이 factory 기본값만 증명 — 편집된 기존 인스턴스 분포 미증명
        evidence: 'docs/adr/906-collection-spacing-runtime-contract.md:127-132'
        root_cause: >-
          round 2 에서 내가 추가한 'props.style.padding/gap 보유 비율 = 0%' 는
          SelectionComponents.ts:322 factory 기본값 (width 만) 근거로 계산됐으나, 이는 신규 생성
          인스턴스만 보장할 뿐 사용자가 Style Panel 로 편집해 저장한 기존 인스턴스 분포는 증명하지 않는다. Codex 독립
          리뷰 (MED 1) 가 포착. round 5 에서 수정: (a) 'factory 기본값으로 신규 생성된 인스턴스 0%' 로
          범위 제한, (b) 편집된 기존 인스턴스 수 N_edited 를 Phase 0 DB scan gate 로 분기, (c)
          N_edited>0 일 때 edited baseline 은 BC 회귀가 아닌 본 ADR 의 핵심 목표 충족 여부로 재분류.
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: adr-structure-violation
        summary: >-
          R1 실패 시 대안의 'builder-local adapter' 가 Decision/G3 의 resolver 공유 조건과 양립
          불가
        evidence: 'docs/adr/906-collection-spacing-runtime-contract.md:119,138'
        root_cause: >-
          round 2 R1 상세 + G1 실패 시 대안에서 'helper 위치를 packages/specs primitive 또는
          builder-local adapter' 옵션을 제시했으나, Decision (ADR:92) 이 render.shapes()
          와 calculateContentHeight() 가 같은 resolver 를 공유하도록 결정했고 G3 (ADR:140) 가
          이를 통과 조건으로 명시한다. builder-local adapter 는 packages/specs/*.spec.ts 의
          render.shapes() 에서 import 불가 (R1 2번 bullet 의 의존 방향 계약) 이므로 옵션 자체가 내부
          모순. Codex 독립 리뷰 (MED 2) 가 포착. round 5 에서 수정: R1 상세 1번 bullet 과 G1 실패 시
          대안 양쪽에서 'builder-local adapter' 제거, 'spec-local export' 로 대체하고 양립 불가
          사유를 명시.
        outcome: fixed
      - id: l3
        severity: LOW
        category: adr-structure-violation
        summary: >-
          G4 Phase 할당이 본문 (Phase 2 일괄) 과 breakdown (Phase 2 TagGroup + Phase 3
          Table) 에서 불일치
        evidence: >-
          docs/adr/906-collection-spacing-runtime-contract.md:141 +
          docs/adr/design/906-collection-spacing-runtime-contract-breakdown.md:79,97
        root_cause: >-
          본문 G4 는 TagGroup/TagList/Table audit 을 Phase 2 에 일괄 고정했으나, breakdown
          Phase 2 는 TagGroup/TagList 만, Phase 3 가 Table 을 분리 처리. Gate 판정 시점 애매.
          Codex 독립 리뷰 (LOW 3) 가 포착. round 5 에서 수정: G4 를 G4a (TagGroup/TagList
          Audit, Phase 2) + G4b (Table Audit, Phase 3) 두 행으로 분할. G4b 실패 시 대안은
          'Table 단독 후속 ADR 로 이관 (R4 scope 폭증 위험 실현 시)' 로 명시하여 R4 와 연결.
        outcome: fixed
  - round: 6
    ts: '2026-04-24T02:19:02.475Z'
    reviewer: codex+claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: adr-structure-violation
        summary: >-
          R1 spec-local export 의 public export surface 조건 누락 (round 5 Codex 지적 →
          round 6 fixed)
        evidence: >-
          docs/adr/906-collection-spacing-runtime-contract.md:119-125,140 +
          docs/adr/design/906-collection-spacing-runtime-contract-breakdown.md:50
        root_cause: >-
          round 5 에서 spec-local export 옵션을 제시했으나
          packages/specs/package.json:exports 가 ./components subpath 미노출 +
          Builder tsconfig 에 @composition/specs/* alias 없음 → direct subpath
          import 취약. round 6 에서 R1 상세에 'Public export surface 계약' bullet 신설,
          Phase 0 G1 검증 명령 2→3개 확장, G1 실패 시 대안에 primitives/* 이관 조건 추가, breakdown
          Phase 0 권장 위치에 root barrel re-export 조건 추가. Codex round 6 재리뷰에서
          package.json:7 + tsconfig.app.json:7 + utils.ts:16 + direct subpath
          import 0건 grep 으로 해소 확증.
        outcome: fixed
      - id: l2
        severity: LOW
        category: adr-structure-violation
        summary: >-
          R2 N_edited 실측 gate 가 breakdown Phase 0 작업/검증 체크리스트에 누락 (round 5 Codex
          지적 → round 6 fixed)
        evidence: >-
          docs/adr/design/906-collection-spacing-runtime-contract-breakdown.md:53-84,191-204
        root_cause: >-
          round 5 에서 ADR 본문만 수정, breakdown 에 실행 항목 누락. round 6 에서 Phase 0
          'N_edited 실측 (R2 baseline 분기 gate)' 하위 섹션 신설 (정의, Supabase SQL 예시,
          Export 파일 scan, 기록 위치, 분기 판정, 통과 조건), 검증 체크리스트를 Phase 0/1/공통/Phase
          2/Phase 3 으로 재편. Codex round 6 재리뷰에서 Supabase elements.tag/props JSONB
          schema + project.schema.ts + supabase.types.ts + factory tag 일치 확증으로
          해소.
        outcome: fixed
      - id: l3
        severity: LOW
        category: adr-structure-violation
        summary: >-
          G4b 'GridList 패턴 이식 가능성 판정' 산출물이 breakdown Phase 3 에 명시 안됨 (round 5
          Codex 지적 → round 6 fixed)
        evidence: >-
          docs/adr/design/906-collection-spacing-runtime-contract-breakdown.md:143-163
        root_cause: >-
          round 5 breakdown Phase 3 출력이 '직접 구현 또는 후속 ADR 분리' 2분기만 명시, 판정 기준/산출물
          모호. round 6 에서 'G4b 통과 산출물' 로 재구조화: (1) Root ↔ Cell padding 소유권 분리 판정표
          4행, (2) GridList 패턴 이식 가능성 판정 기준 4개 (Preview hook point / Skia
          resolver 공유 / Cell padding 안전성 / N_edited_table BC), (3) 최종 판정 3분기 (모두
          O / 일부 / 다수 X). Codex round 6 재리뷰에서 Table.tsx:86/1264 +
          TableRenderer.tsx:343 + Table.spec.ts:156 line 근거 drift 없음 확증으로 해소.
        outcome: fixed
      - id: l4
        severity: LOW
        category: adr-structure-violation
        summary: G1 3번째 검증이 실행 가능한 command 가 아닌 prose (round 6 Codex 신규 지적)
        evidence: 'docs/adr/906-collection-spacing-runtime-contract.md:122-125'
        root_cause: >-
          round 6 에서 Phase 0 G1 검증을 '세 개 모두 조건 충족이어야 PASS' 로 확장했으나, 1~2번은 rg 명령
          형태이고 3번째는 'root barrel re-export 되어 있고 Builder 소비처가 @composition/specs
          root 경로로만 import' 라는 prose 서술. Phase 0 자동/반자동 gate 로 쓰려면 command 형태
          필요. Codex 제안: `rg "resolveGridListSpacingMetric"
          packages/specs/src/index.ts` + `rg
          "@composition/specs/(components|src)" apps/builder packages/shared` 같은
          실행 가능 형태.
        outcome: pending
      - id: l5
        severity: LOW
        category: adr-structure-violation
        summary: G4b 최종 3분기 판정의 경계가 MECE 불완전 (round 6 Codex 신규 지적)
        evidence: >-
          docs/adr/design/906-collection-spacing-runtime-contract-breakdown.md:160-163
        root_cause: >-
          round 6 Phase 3 최종 3분기 ('모두 O / 일부 O 일부 X / 다수 X') 가 상호 배타적이지 않고 망라적이지
          않음. '다수 X' 의 기준이 3/4 인지 2/4 인지 불명 → 2O/2X 같은 결과가 '일부' 와 '다수' 두 분기에 걸칠
          가능성. Codex 제안: '4O / 1-3O / 0O' 또는 '4O / 2-3O / 0-1O' 처럼 수치 기준으로 고정.
        outcome: pending
  - round: 7
    ts: '2026-04-24T02:21:33.361Z'
    reviewer: claude
    source: live
    issues:
      - id: l1
        severity: LOW
        category: adr-structure-violation
        summary: >-
          G1 3번째 검증을 prose 에서 rg command 로 변환 (round 6 Codex LOW 1 → round 7
          fixed)
        evidence: 'docs/adr/906-collection-spacing-runtime-contract.md:122-126'
        root_cause: >-
          round 6 에서 G1 검증 3번째 항목이 'root barrel re-export 되어 있고 Builder 소비처가
          @composition/specs root 경로로만 import' 라는 prose. round 7 에서 두 개의 실행 가능한
          rg command 로 분해: (3) `rg "<resolver 심볼명>" packages/specs/src/index.ts`
          ≥ 1건 (root barrel re-export 확인), (4) `rg
          "@composition/specs/(components|src)" apps/builder packages/shared` =
          0건 (비공개 subpath 직접 import 차단). G1 검증 명령이 3→4개로 확장되었으며 전부 rg 자동화 가능.
        outcome: fixed
      - id: l2
        severity: LOW
        category: adr-structure-violation
        summary: G4b 최종 3분기 판정에 수치 기준 고정 (round 6 Codex LOW 2 → round 7 fixed)
        evidence: >-
          docs/adr/design/906-collection-spacing-runtime-contract-breakdown.md:160-163
        root_cause: >-
          round 6 Phase 3 최종 3분기 ('모두 O / 일부 / 다수 X') 가 상호 배타적이지 않고 경계 모호. round
          7 에서 4개 항목 중 O 개수 기준으로 수치 고정: 4/4 O → 직접 구현, 2/4 또는 3/4 O → 부분 이식 + 후속
          ADR, 0/4 또는 1/4 O → Table 단독 후속 ADR. 'MECE (상호 배타적이며 망라적)' 명시 추가. 2 이상
          O = 부분 이식 경제성 확보, 1 이하 O = 후속 ADR 이관이 실용적이라는 경계 선택.
        outcome: fixed
---
# ADR-906 Review Log

## Round 1 — 2026-04-24 (reviewer: claude)

## ADR 리뷰 결과: ADR-906 Collection spacing 런타임 계약

### 구조 검증

9/9 섹션 Risk-First 템플릿 완전 준수. Context (hard constraints 5개 + SSOT D3 명시) / Alternatives(3개 A/B/C) / 4축 위험 평가 / Risk Threshold Check / Decision (위험 수용 + 기각 사유) / 구현 상세 분리 (breakdown 포인터) / Risks (ID 표 R1-R4, Decision 뒤·Gates 앞) / Gates (G1-G5, R1 HIGH→G1 매핑) / Consequences 모두 PASS + HIGH 품질.

### 코드 검증 (6/6 VERIFIED)

1. renderGridList root style 미전달 — `SelectionRenderers.tsx:617-644` (대조 ListBox line 218, 304)
2. GridList component props.style 병합 준비 — `GridList.tsx:33,58,126-151`
3. data-layout 속성 미명시 — `GridList.css:30` + `GridList.tsx` 6개 AriaGridList 반환 경로 (147/177/214/229/244/274)
4. GridListSpec.render.shapes의 size.gap 소비 — `GridList.spec.ts:251`
5. calculateContentHeight의 style.gap/padding 소비 — `utils.ts:1578, 402-421`
6. ListBox 선례 — `ListBox.spec.ts:377-397` (ADR-078 Phase 5 fix #2)

### 위험 평가

- R1 (HIGH): package boundary 위반 → G1 대응 매핑
- R2-R4 (MED): 기재 + 대응 명시
- 누락 위험: 0건 (layoutVersion 3-심볼 해당 없음, Preview postMessage 동기화는 상위 ADR-082 체인)

### Seed 체크

- #1 코드 경로 3곳+ 구체 인용: PARTIAL — References 9 경로 + breakdown 으로 보강, 본문 직접 인용은 추상적
- #2 Generator 확장 선언: N/A (runtime contract 범위, Generator 확장 아님)
- #3 BC 수식화: PARTIAL — 원칙 명시, X% 수치 없음
- #4 Phase 분리 가능성: PASS — Phase 1/2/3 분리 + R4 Table Phase 3 audit 후 후속 ADR 분리 여지 명시

### 종합 판정

- 구조: PASS (9/9 HIGH)
- 코드 정합: 6/6 VERIFIED
- 누락 위험: 0건
- HIGH: 없음
- MEDIUM: 없음
- LOW: 2건 (L1 R1 boundary 구체화, L2 R2 BC 수식화)
- **결론: 승인 가능**. Phase 1 착수 가능, LOW 2건은 문서 선명도 개선 수준으로 차단 요인 아님.

## Round 2 — 2026-04-24 (reviewer: claude)

## Round 2 — LOW 2건 보완

### L1 fixed: R1 상세 섹션 신설 (`ADR-906:117-123`)

- helper 배치 옵션 명시: `packages/specs/src/primitives/*` 또는 builder-local adapter
- 의존 방향 계약 명시: `packages/specs ← packages/shared ← apps/builder`
- Phase 0 G1 검증 명령 2건 (`rg` 로 specs → apps/builder, specs → @composition/shared import 0 건 확인)

### L2 fixed: R2 상세 섹션 신설 (`ADR-906:125-130`)

- 수치 근거: 현 factory 기본 style = `{ width: "100%" }` 단독 (`SelectionComponents.ts:322-324`)
- BC 수식화: 기존 GridList 인스턴스 중 `props.style.padding/gap` 보유 비율 = **0%** → 모든 기존 인스턴스가 신규 파이프라인 `props.style` 부재 경로 baseline 대상
- G3 통과 조건 수치 고정: `paddingTop=paddingRight=paddingBottom=paddingLeft=0` + `gap=12` 일 때 `render.shapes()` 좌표와 `calculateContentHeight()` 가 현 main 과 byte-equal (fixture test)
- 사용자 편집 인스턴스는 마이그레이션 없이 즉시 새 metric 반영

### 부수 개선

- Risks 표의 `R1`/`R2` 대응 칸을 'R1 상세/R2 상세 섹션 참조' 포인터로 축소 → markdown 테이블 formatter 간섭 회피 (인라인 코드 + 한글 경계 공백 흡수 문제 근본 해결).

### 종합 판정 (round 2 후)

- 구조: PASS (10 섹션 HIGH, R1/R2 상세 분리)
- 코드 정합: 6/6 VERIFIED 유지
- 누락 위험: 0건
- HIGH: 없음
- MEDIUM: 없음
- LOW: 0건 (round 1 의 2건 모두 fixed)
- **결론: 승인 가능 — 후속 보완 없음, Phase 1 착수**

## Round 3 — 2026-04-24 (reviewer: claude)

## Round 3 — 심층 재검증에서 MED 1건 신규 발견

### Round 2 LOW 2건 확인

- L1 (R1 boundary 상세, `ADR-906:117-123`): VERIFIED — 3 rg 모두 0건 확증 (specs → apps/builder / @composition/shared / ../shared 역방향 import 없음)
- L2 (R2 BC 수식화, `ADR-906:125-130`): VERIFIED — factory `{ width: "100%" }` 단독 + gap=12 양쪽 fallback (GridList.spec.ts:213/251, utils.ts:1599)

### MED 신규 발견 (R5)

**원문**: `ADR-906:15` 주장 3 — "GridList.css 는 [data-layout="grid"] selector 를 기대하지만 component 는 data-layout 을 명시하지 않는다"

**실측**: `react-aria-components@1.15.1/dist/GridList.mjs:198` 에서 RAC 내부 root `<div>` 가 `"data-layout": layout` 속성을 무조건 auto-emit.

**영향 3곳**:
- M1: ADR Context 주장 3 서술이 독자 오도
- M2: breakdown Phase 1 'data-layout 수동 추가' 작업이 중복
- M3: G2 Gate 조건 중 data-layout 부분이 RAC 기본동작 검증으로 퇴화

### 수정 제안

- `ADR-906:15` 주장 3 재서술 (RAC auto-emit 인정 + 실결함 = style 전달 누락)
- `breakdown:58-59` Phase 1 data-layout 추가 지시 제거 또는 'RAC auto-emit 확인만' 으로 교체
- `ADR-906:137` G2 조건에서 data-layout 부분 제거 또는 약화

### 자기 반성

Round 1 에서 주장 3 을 VERIFIED 로 판정한 것은 composition `GridList.tsx` grep 에서 data-layout 문자열 부재만 확인하고, 상류 RAC 구현을 추적하지 않은 검증 공백. xhigh effort 재검증에서 해소.

### 종합 판정 (round 3 후)

- 구조: PASS (round 2 보완 반영)
- 코드 정합: 5/6 VERIFIED + 1/6 UNVERIFIED (주장 3)
- 누락 위험: 1건 신규 (MED)
- HIGH: 없음
- MEDIUM: 1건 (R5 — 3곳 동기화 수정 필요)
- LOW: 0건 (round 1 두 건 모두 round 2 에서 fixed)
- **결론: 수정 후 재리뷰 권장** — Phase 1 착수 전 M1-M3 동기화 수정

## Round 4 — 2026-04-24 (reviewer: claude)

## Round 4 — MED 1건 fixed

### 수정 3곳 (동기화 완료)

- **M1** `ADR-906:15` Context 주장 3 재서술 — RAC 내부 root div 의 `data-layout` auto-emit 을 인정하고, 이 부분은 결함이 아니며 실결함은 `renderGridList` 의 root `element.props.style` 미전달임을 명시. 증거 인용: `react-aria-components@1.15.1 dist/GridList.mjs:198`.
- **M2** `breakdown:58-59` Phase 1 수정 — 'GridList.tsx 의 모든 AriaGridList 반환 경로에 data-layout 수동 추가' 지시 삭제. 'RAC auto-emit 으로 composition 쪽 변경 없이 CSS selector 매칭' 으로 교체. 기존 `style={gridListStyle}` 유지 조항은 잔존.
- **M3** `ADR-906:137` G2 Gate 조건 재정의 — '`style.padding/gap` Preview inline style 반영 + RAC auto-emit `data-layout` 과 `GridList.css` selector 매칭이 test 로 확인 (수동 추가 아님)'. 실패 시 대안도 'renderer root props 계약 재검토' 로 축약 (component root props 는 변경 필요 없음).

### 영향 범위

- Phase 1 구현 범위 축소: `GridList.tsx` 편집 불필요 → `SelectionRenderers.tsx` root style 전달 + `GridListSpec.render.shapes` padding/gap 소비 + `utils.ts` gridlist branch resolver 공유 세 가지로 집약.
- G2 test 성격 변경: 'data-layout 이 DOM 에 달렸는가' (RAC 기본동작) 검증으로 **퇴화 방지** — test 자체는 유지하되 'auto-emit 확인' 목적으로 문서화.
- R2 BC 수식화(round 2 에서 이미 고정) 와 충돌 없음 — factory style 은 여전히 `{ width: "100%" }` 단독.

### 최종 판정 (round 4 종결)

- 구조: PASS (10 섹션 HIGH, round 2/4 보완 반영)
- 코드 정합: 6/6 VERIFIED (주장 3 재서술으로 UNVERIFIED 해소)
- 누락 위험: 0건
- HIGH: 없음
- MEDIUM: 0건 (round 3 의 1건 fixed)
- LOW: 0건 (round 1 의 2건 round 2 에서 fixed)
- **결론: 승인 가능 — Phase 1 착수 가능**

## Round 5 — 2026-04-24 (reviewer: codex+claude)

## Round 5 — Codex 독립 리뷰 3건 fixed (MED 2 + LOW 1)

### 배경

Round 4 종결 (Claude 판정 '승인 가능') 후 Codex 가 Bash 직접 호출 경로로 독립 리뷰 수행. Claude 가 놓친 3건 (MED 2 + LOW 1) 포착 — 모두 타당한 지적으로 수락.

### 수정 1 (MED) — R2 '0%' 단정 범위 제한

- **원래 문구** (`ADR-906:128`): '기존 저장 프로젝트의 GridList 인스턴스 중 props.style.padding/gap 보유 비율 = 0%'
- **결함**: factory 기본값만 증명, 사용자 편집 분포 미증명
- **수정**: 'factory 기본값으로 신규 생성된 인스턴스 0%' 로 범위 제한 + Phase 0 DB scan 으로 `N_edited` 측정 + fixture baseline (a) unedited / (b) edited 분기 + edited baseline 은 BC 회귀 아니라 본 ADR 핵심 목표 충족 여부로 재분류

### 수정 2 (MED) — R1 resolver 위치 옵션 정합화

- **원래 문구** (`ADR-906:119`, `:138`): 'packages/specs primitive 또는 builder-local adapter'
- **결함**: builder-local adapter 는 packages/specs 의 render.shapes() 에서 import 불가 → G3 resolver 공유 조건과 양립 불가
- **수정**: R1 상세 + G1 실패 시 대안 양쪽에서 'builder-local adapter' 제거, 'spec-local export' 로 대체 + 양립 불가 사유 명시

### 수정 3 (LOW) — G4 Phase 분할

- **원래 구조** (`ADR-906:141`): G4 'TagGroup/TagList/Table audit' Phase 2 일괄
- **결함**: breakdown 은 TagGroup/TagList Phase 2 + Table Phase 3 분리
- **수정**: G4 를 G4a (TagGroup/TagList, Phase 2) + G4b (Table, Phase 3) 두 행으로 분할 + G4b 실패 시 대안으로 'Table 단독 후속 ADR 로 이관 (R4 실현 시)' 명시하여 R4 와 연결

### 종합 판정 (round 5 종결)

- 구조: PASS (Gate 5→6개 분할, R2 baseline 분기 명시)
- 코드 정합: Codex 7/7 VERIFIED (renderGridList + GridList.tsx + RAC auto-emit + GridListSpec + utils + TagGroup + Table)
- 누락 위험: 0건
- HIGH: 없음
- MEDIUM: 0건 (Codex 2건 fixed)
- LOW: 0건 (Codex 1건 fixed + round 1 2건 round 2 fixed + round 3 1건 round 4 fixed)
- **결론: 승인 가능 — Phase 0 실측 gate (N_edited DB scan) 수행 후 Phase 1 착수**

### 리뷰 프로세스 교훈

- Round 1~4 는 Claude 단독 review-adr. Round 3 에서 RAC auto-emit 미확인 공백 자체발견 → round 4 수정.
- Round 5 는 Codex 독립 리뷰가 Claude 가 놓친 3건 추가 포착. 특히 'factory 기본값 → 기존 인스턴스 0% 확장' 논리 비약은 Claude 자체 재검증에서 놓친 유형.
- 결론: 복합 ADR 은 Claude + Codex 교차 리뷰가 단일 reviewer 대비 결함 포착률 유의미하게 높음. feedback-codex-invocation-bash-direct.md 로 향후 워크플로 고정.

## Round 6 — 2026-04-24 (reviewer: codex+claude)

## Round 6 — Codex 재리뷰: 승인 가능 + 새 LOW 2건

### Round 5 지적 3건 모두 해소 확증

Codex 재리뷰가 다음을 확증:

- R1 public export surface: `packages/specs/package.json:7` exports 구조 확인 + `tsconfig.app.json:7` alias 부재 확인 + `utils.ts:16` 기존 root import 패턴 일치 + direct subpath import 0건 grep
- N_edited SQL/schema: `SUPABASE.md:19` elements.tag/props JSONB + `project.schema.ts:55` + `supabase.types.ts:121` + factory tag 일치 (`SelectionComponents.ts:313`) 전원 확인
- Table 판정표 line drift: `Table.tsx:86/1264` + `TableRenderer.tsx:343` + `Table.spec.ts:156` 모두 현 코드와 일치

### Round 6 신규 LOW 2건

**LOW 1 — G1 3번째 검증 실행성**: `ADR-906:122-125`. 1~2번은 `rg` command 지만 3번째는 prose 서술 → Phase 0 gate command 형태 필요.

**LOW 2 — G4b MECE 불완전**: `breakdown:160-163`. '모두 O / 일부 O 일부 X / 다수 X' 경계 모호 → 수치 기준 고정 필요.

### 종합 판정 (round 6)

- 구조: PASS
- 코드 정합: Codex 7/7 VERIFIED 유지
- Round 5 지적: 3/3 fixed
- Round 6 신규: 0 HIGH / 0 MED / 2 LOW
- **결론: 승인 가능** — Phase 0 착수 전 LOW 2건 선명도 개선 권장 (차단 요인 아님)

### 리뷰 프로세스 교훈

- Round 5→6 Codex 재리뷰 사이클: 원 지적 3건 전원 fixed 확증 + 새 LOW 2건 포착. Claude 단독 리뷰는 round 4 까지 '승인 가능' 판정이었으나 Codex 가 round 5/6 에서 추가 결함 포착 — 2 reviewer 교차 패턴의 가치 재확증.
- LOW 2건 모두 'Phase gate 실행성/MECE' 유형. 계약 논리 결함이 아니라 gate 운영 선명도 문제. Phase 1 착수는 이 2건 없이도 가능하나 반영 시 재리뷰 부담 감소.

## Round 7 — 2026-04-24 (reviewer: claude)

## Round 7 — Codex LOW 2건 fixed

### 수정 1 (LOW 1) — G1 3번째 검증을 rg command 화

**이전** (`ADR-906:122-125`, round 6):
```
- Phase 0 G1 검증 명령 (세 개 모두 조건 충족이어야 PASS):
  - `rg "from ['\"](\.\./)+apps/builder" packages/specs` = 0건
  - `rg "from ['\"]@composition/shared" packages/specs` = 0건
  - spec-local helper 가 ... re-export 되어 있고, Builder 소비처가 ... root 경로로만 import
```

**이후** (round 7):
```
- Phase 0 G1 검증 명령 (네 개 모두 조건 충족이어야 PASS, 전부 rg 로 자동화 가능):
  - `rg "from ['\"](\.\./)+apps/builder" packages/specs` = 0건
  - `rg "from ['\"]@composition/shared" packages/specs` = 0건
  - `rg "<resolver 심볼명>" packages/specs/src/index.ts` ≥ 1건 (root barrel re-export 확인)
  - `rg "@composition/specs/(components|src)" apps/builder packages/shared` = 0건 (비공개 subpath 차단)
```

3번째 prose 를 두 개의 실행 가능한 rg command 로 분해 — Phase 0 자동/반자동 gate 로 실행 가능.

### 수정 2 (LOW 2) — G4b 3분기 수치 기준 고정

**이전** (`breakdown:160-163`, round 6):
```
- 모두 O → 직접 구현
- 일부 O 일부 X → 부분 이식
- 다수 X → Table 단독 후속 ADR
```

**이후** (round 7):
```
- 4/4 O → 직접 구현
- 2/4 또는 3/4 O → 부분 이식 + 후속 ADR 로 잔여 이관
- 0/4 또는 1/4 O → Table 단독 후속 ADR 로 전체 이관
```

MECE (상호 배타적 + 망라적) 명시. 2 이상 O 에서 '부분 이식 경제성 확보' / 1 이하에서 '후속 ADR 이관 실용' 경계.

### 종합 판정 (round 7 종결)

- 구조: PASS
- 코드 정합: Codex round 6 7/7 VERIFIED 유지
- 누락 위험: 0건
- HIGH: 없음
- MEDIUM: 0건 (round 1-6 모든 이슈 fixed)
- LOW: 0건 (round 6 Codex LOW 2 건 fixed)
- **결론: 최종 승인 — Phase 0 착수 가능, 재리뷰 불필요**

### 리뷰 이력 총괄 (round 1~7)

- round 1 (Claude): 구조 PASS + LOW 2 (R1 구체성 / R2 수식화)
- round 2 (Claude): LOW 2 fixed → 새 MED 0 (판정 '승인 가능')
- round 3 (Claude 심층 재검증): 새 MED 1 발견 (RAC auto-emit 공백)
- round 4 (Claude): MED 1 fixed → 판정 '승인 가능'
- round 5 (Codex 독립): 새 MED 2 + LOW 1 발견 (0% 단정 / R1 spec-local / G4 phase)
- round 6 Pre-Codex (Claude 반영): MED 2 + LOW 1 fixed
- round 6 Codex 재리뷰: fixed 3/3 확증 + 새 LOW 2 (G1 실행성 / G4b MECE)
- **round 7 (Claude): LOW 2 fixed → 최종 승인**

총 7-round, 8건 이슈 (MED 3 + LOW 5) 전원 fixed. Claude 단독 4 round 이후 Codex 2 round 추가로 catch 한 결함 3건 (MED 2 + LOW 1) + Codex 재리뷰가 추가 catch 한 LOW 2건 = 교차 검증의 정량 기여 5건.
